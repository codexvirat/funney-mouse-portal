// Escape text before putting it into print HTML (innerHTML). Notes and
// names can come from customers via QR ordering.
export function esc(v) {
  return String(v == null ? '' : v).replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]));
}
